name: Appium Mobile Tests

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run the tests against'
        required: true
        default: 'QA'
        type: choice
        options:
          - QA
          - STAGE
          - PROD
      headless:
        description: 'Run Emulator in Headless mode'
        required: true
        default: true
        type: boolean

jobs:
  mobile-tests:
    runs-on: macos-latest # <--- Uses a fresh GitHub-hosted Mac runner
    environment: ${{ inputs.environment }} 
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: 1. Setup Appium & Dependencies
        uses: ./.github/actions/setup-appium
        with:
          environment: ${{ inputs.environment }}

      - name: 2. Run Emulator & Tests
        uses: ./.github/actions/run-tests
        with:
          headless: ${{ inputs.headless }}

      - name: 3. Generate Reports & Artifacts
        if: always() # Runs even if the tests fail
        uses: ./.github/actions/allure-report
